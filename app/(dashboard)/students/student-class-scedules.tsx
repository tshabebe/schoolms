"use client";

import { api } from "@/app/lib/trpc/client";

export default function StudentScedule() {
  // const scedule = api.sceduleRouter.getStudentScedule.useQuery();
  // console.log(scedule.data);
  return <div>hello</div>;
}
